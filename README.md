# King Star

Project for product research and selling on Amazon.

## Usage

Install dependencies for backend:

```bash
pip install -r requirements.txt
```

Install frontend dependencies (requires Node.js and npm):

```bash
cd apps/frontend
npm install
```

Run both backend and frontend on Windows:

```
start.bat
```
